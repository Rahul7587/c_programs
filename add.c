#include <stdio.h>
int main() {
    float a,b,sum;
    printf("welcome to my addition calculator\n");
    scanf("%f %f",&a,&b);
    sum=a+b;
    printf("the sum of the given numbers is %f",sum);
    return 0;
}